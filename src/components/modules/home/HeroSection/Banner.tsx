import styles from "./HeroSection.module.css";

const Banner = () => {
  return (
    <div
      className={`${styles.banner} container mx-auto my-20 border-2 border-white rounded-3xl`}
    ></div>
  );
};

export default Banner;
